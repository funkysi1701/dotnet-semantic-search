# No services by default. Embeddings are stored in Azure Cosmos DB for NoSQL (see README).
name: dotnet-semantic-search
services:
  readme-only:
    image: alpine:3.20
    command: ["true"]
    profiles: ["unused"]
